# 2026-04-30 작업 로그

## 오늘 한 작업
- 모바일 `settings` 탭에서 API Base URL 직접 입력/저장 기능 추가
- 저장한 API 주소를 `SecureStore`/`localStorage`에 유지하도록 설정 저장 로직 추가
- 모바일 API 클라이언트가 고정 상수 대신 저장된 API 주소를 요청 시점마다 읽도록 변경
- `settings` 화면에 현재 적용 URL, source, default URL 표시 추가
- `settings` 화면에 기본값 초기화 버튼과 실기기 테스트용 안내 문구 추가
- 개발 모드에서만 API 설정 카드가 보이도록 `settings` 화면 분리
- 운영 기준에서는 `settings`가 사용자용 placeholder 화면처럼 보이도록 문구 정리
- `settings` 탭을 사용자 설정 중심 화면으로 재구성
- 계정 상태 표시, 로그인/계정 전환, 세션 새로고침, 로그아웃 버튼 추가
- 녹음 기본 옵션 저장 기능 추가
  - 기본 세션 제목
  - 녹음 종료 후 자동 업로드
  - 업로드 후 결과 상세 자동 열기
- 저장한 녹음 기본 옵션이 `record` 화면에 실제 반영되도록 연결
- 모바일 공통 저장 유틸(`storage.ts`) 추가로 설정/세션 저장 로직 중복 정리
- 실기기 테스트 전용 모바일 QA 체크리스트 문서 추가
- `settings`에 학습 기본 옵션 추가
  - 기본 답변 모드
  - 질문 한국어 TTS 자동 재생
  - 질문 후 자동 녹음 시작
- `reviews`가 저장된 학습 설정을 읽어 기본 답변 모드와 자동 TTS/자동 녹음 동작에 반영되도록 연결
- `home` 탭을 계정/세션 관리 화면에서 모바일 진입 허브 화면으로 재구성
- `expression/[id]/practice`도 저장된 학습 설정을 읽어 기본 답변 모드와 자동 TTS/자동 녹음 동작에 반영되도록 연결
- `recording/[id]` 편집 UX 개선
  - 전체 / 내 화자 / 다른 화자 필터 추가
  - 문장 미저장 변경 표시 추가
  - 문장별 연결된 표현 미리보기 추가
  - 생성된 표현 / 연결 표현 상세 화면 바로가기 추가
  - 기본 정보 카드에 생성된 표현 수 표시 추가
- 모바일 QA 문서를 `모바일 웹/개발 프리뷰`와 `실기기 전용` 체크 항목으로 재구성
- `record` 화면 상태/버튼 문구 정리
  - 상단 현재 상태 요약 카드 추가
  - 다음 액션 안내 문구 추가
  - 업로드/세션 제어 버튼 라벨을 한국어 기준으로 정리
  - 수동 세션 제어와 기본 업로드 흐름의 역할을 더 명확하게 분리
- `expressions` 목록/상세 학습 진입 UX 정리
  - 목록에 빠른 필터와 추천 표현 구간 추가
  - TTS 필요 / 복습 우선 / 최근 생성 기준으로 표현 우선순위 확인 가능
  - 표현 상세에 다음 학습 액션 안내 카드 추가
  - 표현 상세에서 바로 연습하기 / 정답 먼저 듣기 액션 강조
  - 메모 미저장 변경 표시 추가
- `reviews` 와 `expression practice` 피드백 카드 구조 통일
  - 총점 아래 세부 점수를 칩 형태로 정리
  - STT 인식 / 총평 / 잘한 점 / 교정 포인트 / 추천 답변 순서로 읽히도록 구조화
  - 추천 답변과 대안 답변을 별도 강조 박스로 분리
- `expressions`에 `TTS 완료만 보기` 필터와 목록 내 개별 TTS 재생 추가
- `expressions`에 `TTS 완료` 기준 전체 연속 재생 추가
- `expressions` 목록/추천 카드에서 `표현 연습` 빠른 이동 추가
- `expressions` 전체 연속 재생에 `한 번 더 반복` / `문장 사이 텀` 옵션 추가
- 웹 `음성데이터 수집 / 텍스트 정리` 흐름을 참고해 모바일 `recording/[id]` 기능 보강
  - 대화 맥락 힌트 로컬 저장 및 최근 맥락 다시 사용 추가
  - 관련 인물 선택, 관계 / 상황 / 톤 템플릿 입력 추가
  - 대화 요약 / 문장 의도 분석 실행 버튼 추가
  - 분석 상태와 변경 사유 표시, `이상없음으로 표시` 액션 추가
  - 화자별 이름 변경, 화자별 인물 연결 UI 추가
  - 발화별 화자 재지정, 맥락 메모 저장, 분석 의도 확인 UI 추가
  - 모바일 녹음 상세가 웹의 맥락 저장 / 인물 연결 / 발화 정리 흐름을 1차로 따라가도록 정리
- 모바일 recording API 확장
  - recording 상세 응답에 participants / speakerProfiles / analysisUpdatedAt 타입 반영
  - recording analyze / participants / speaker-label / speaker-profile / analysis-status API 함수 추가
- 모바일 person profile 조회 API와 recording context 저장 헬퍼 추가
- 모바일 `recording/[id]` 추가 보강
  - 발화 카드에서 원본 오디오 구간 바로 듣기 / 정지 추가
  - 녹음 상세용 분석 실행 방식 설정 추가
  - `수동 / 자동` 모드 저장 후, 자동 모드에서는 맥락 저장 / 문장 저장 뒤 대화 요약과 의도 분석 자동 재실행
- 모바일 recording analysis preference 저장 헬퍼 추가
- 웹 recording detail 기준 기능 추가 정렬
  - 모바일 `recording/[id]`에 `내 문장 / 기타 화자 일괄 표현 생성` 추가
  - `내 화자 지정`, `화자 이름 변경` 후 자동 분석 모드일 때 대화 요약 / 의도 분석 자동 재실행
- 모바일 expressions API에 recording 단위 bulk expression generation 함수 추가
- 웹 recording detail 기준 `남은 TTS 일괄 생성`도 모바일 `recording/[id]`에 추가
- 모바일 expressions API에 recording 단위 bulk TTS generation 함수 추가
- 모바일 `recording/[id]` 원본 오디오 재생 위치 표시 추가
  - 현재 재생 중인 클립의 경과 시간 / 전체 길이 표시
  - 상단 진행 바와 발화 카드별 클립 진행 상태 표시
- 모바일 `Record` 탭에 최근 녹음 브라우저 추가
  - 저장된 녹음 목록 조회
  - 카드별 `열기 / 다시 처리 / 삭제` 액션 추가
  - 웹의 `이전 녹음 불러오기` 흐름을 모바일에서는 `Record` 탭 하단 최근 녹음 섹션으로 재구성
- 모바일 recordings API에 목록 / 재처리 / 삭제 함수 추가
- 모바일 최근 녹음 브라우저 추가 다듬기
  - 파일명 / 상태 기반 검색 추가
  - `전체 / 처리 완료 / 처리 중 / 재검토 필요` 필터 추가
  - 카드에 마지막 분석 상태 표시 추가
- `Settings`에 `개인 인물 사전` 진입 카드 추가
- 모바일 `개인 인물 사전` 화면 추가
  - 등록된 인물 목록 / 검색
  - 새 인물 추가
  - 인물 수정
  - 인물 삭제
  - `isMe` 포함한 역할 / 관계 / 별칭 / 메모 관리
- 모바일 person profile API에 create / update / delete 함수 추가
- `Home` 탭에 `Quick Save` 진입 카드 추가
- `Expressions` 탭 상단에 `빠른 문장 저장` 진입 카드 추가
- 모바일 `quick-sentence` 전용 화면 추가
  - 한국어 문장 빠른 입력
  - `맥락 추가` 접힘 섹션
  - 관련 인물 선택
  - 최근 맥락 다시 사용
  - 관계 / 상황 / 톤 템플릿 입력
  - `저장 후 표현 생성` 후 expression 상세로 바로 이동
- 모바일 expressions API에 `koreanText` 기반 직접 표현 생성 함수 추가
- 모바일 AI 대화 설계 문서 보강
  - 영어 트랙을 `실전 연습 + 영어 자산화 + 다이얼로그 저장` 관점으로 재정의
  - 한국어 트랙을 `빠른 문장 저장`, `대화 녹음`과 나란한 `한국어 수집 채널`로 정리
  - `Home -> AI Conversation Home -> Conversation Session` 구조와 세부 MVP 단계(M1~M6) 정리
  - 모바일 MVP 티켓의 `Ticket M`을 구현 단위 기준으로 더 잘게 분해
- 모바일 AI 대화 1차 화면 구현 시작
  - `Home`에 `AI Conversation` 진입 카드 추가
  - `AI Conversation Home` 화면 추가
  - 영어/한국어 트랙별 최근 세션 목록 조회 추가
  - 트랙별 `새 대화 시작` 준비 화면 추가
  - 세션 생성 후 읽기 전용 `AI Conversation Session` 상세 화면 추가
  - 텍스트 대화 전송 전 단계로 세션 메타데이터와 기존 턴을 먼저 확인할 수 있게 구성
- 모바일 AI 대화 `M2/M3` 구현
  - 세션 상세 화면에 텍스트 입력창과 `AI에게 보내기` 추가
  - `POST /ai-conversations/respond` 연결로 실제 텍스트 대화 턴 생성
  - 사용자 턴에 영어 트랙 `영어 표현 생성`, 한국어 트랙 `저장 후 영어 표현 생성` 액션 추가
  - 턴 저장 후 expression 상세로 바로 이동하도록 연결
- 모바일 AI 대화 `M4` 구현
  - 세션 상세 화면에 음성 답변 녹음 / 종료 / STT 전사 흐름 추가
  - `POST /ai-conversations/transcribe` 연결로 영어/한국어 음성 답변 전사 추가
  - AI 음성 응답 자동 재생과 턴별 `AI 음성 듣기` 버튼 추가
  - 영어 트랙에서만, 그리고 `AI 음성 응답`일 때만 `AI 영문 표시` 옵션 노출
  - `apiFetch`가 FormData 요청도 처리할 수 있도록 공통 클라이언트 보강
- 모바일 AI 대화 `M5` 1차 구현
  - 영어 세션 상세에 `다이얼로그 연습으로 저장` 액션 추가
  - 웹과 같은 `POST /ai-conversations/sessions/:id/dialogue-practice` API 재사용
  - 현재 세션에서 생성된 다이얼로그 연습 세트 목록 표시
  - 각 세트에서 첫 질문 텍스트와 첫 질문 TTS 바로 듣기 추가
- 모바일 다이얼로그 연습 플레이어 1차 구현
  - `dialogue-practice/[id]` 전용 화면 추가
  - AI 질문 보기/숨기기, 질문 TTS 재생, 현재 turn 진행 표시 추가
  - 텍스트 답변 / 음성 답변(STT) 전환 추가
  - 음성 녹음 -> STT 전사 -> 답변 초안 반영 흐름 추가
  - 정답/대안/힌트/설명 보기와 이전/다음 turn 이동 추가
- 모바일 AI 대화 / 다이얼로그 STT 파일명 보강
  - 음성 전사 요청 전에 `m4a/webm` 등 안전한 확장자가 붙도록 파일명 보정 추가
  - OpenAI STT 호출 직전에도 서버에서 한 번 더 파일명 확장자 보정
  - 모바일 음성 전사 시 `Unsupported file format` 오류가 나던 경로를 방어
- 모바일 AI 대화 / 다이얼로그 STT content-type 정규화 추가
  - Expo recording status의 generic `audio` 값을 그대로 쓰지 않고 `audio/mp4` 같은 실제 MIME으로 보정
  - 파일명 확장자 보정과 함께 STT multipart 업로드의 형식 안정성 강화
- 모바일 AI 대화 / 다이얼로그 STT 확장자-실파일 포맷 mismatch 보정
  - `audio/webm`인데 `.m4a`로 남아 있던 경우, content-type 기준으로 파일 확장자를 강제로 교체
  - 서버에서도 buffer header를 보고 `webm / wav / mp3 / m4a` 확장자를 다시 맞추도록 보강
- 모바일 영어 AI 대화 `도움 요청` 추가
  - 웹과 같은 `POST /ai-conversations/assist-reply` API 재사용
  - 한국어 의도 입력 -> `easy / natural / note` 후보 표시
  - 추천 답변을 현재 영어 답변 초안으로 바로 채우기 추가
- 모바일 한국어 AI 트랙 후속 학습 연결 보강
  - `저장 후 영어 표현 생성` 직후 세션 화면에서 `표현 보기 / 바로 연습 / 오늘 복습` 액션 제공
  - 한국어 수집 채널에서 생성된 표현이 학습 흐름으로 더 짧게 이어지도록 정리
- 모바일 AI 대화 관리 UX 보강
  - 세션 상세에서 제목 수정/저장 추가
  - `AI Conversation Home`에 최근 세션 검색 추가
  - 최근 수정순 / turn 많은 순 정렬 추가
- 모바일 AI 대화 세션 화면을 웹처럼 더 위에서 아래로 읽히는 구조로 재배치
  - 대화 턴 스레드를 먼저 보여주고 `현재 답변` 카드를 그 아래에 배치
  - 영어 트랙 `도움 요청`을 별도 큰 카드 대신 현재 답변 내부 펼침 섹션으로 이동
  - 한국어 트랙 후속 학습 카드와 영어 트랙 `다이얼로그 연습으로 저장` 카드를 답변 이후 단계로 정리
- AI 대화 STT 업로드 디버그 로그 추가
  - 서버에서 `originalname / mimetype / size / buffer header`를 바로 확인할 수 있게 로그 추가
  - 실기기 재현 시 실제 업로드 포맷을 바로 좁힐 수 있게 준비

## 테스트 / 검증
- `pnpm --filter @elp/mobile exec tsc --noEmit`

## 수정한 주요 파일
- `apps/mobile/app/(tabs)/home.tsx`
- `apps/mobile/app/(tabs)/settings.tsx`
- `apps/mobile/app/(tabs)/reviews.tsx`
- `apps/mobile/app/(tabs)/record.tsx`
- `apps/mobile/app/(tabs)/expressions.tsx`
- `apps/mobile/app/quick-sentence.tsx`
- `apps/mobile/app/person-profiles.tsx`
- `apps/mobile/app/expression/[id]/practice.tsx`
- `apps/mobile/app/recording/[id].tsx`
- `apps/mobile/src/lib/recording-analysis-preference.ts`
- `apps/mobile/src/lib/api/expressions.ts`
- `apps/mobile/src/lib/api/person-profiles.ts`
- `apps/mobile/src/lib/api/recordings.ts`
- `apps/mobile/src/lib/config.ts`
- `apps/mobile/src/lib/recording-context.ts`
- `apps/mobile/src/lib/api/client.ts`
- `apps/mobile/src/lib/learning-preferences.ts`
- `docs/mobile_device_qa_checklist.md`

## 메모
- 이제 앱 재시작 없이 저장 직후부터 새 API 주소로 요청이 나간다
- 기본값은 기존과 동일하게 `env -> 플랫폼 기본값 -> 운영 fallback` 순서로 결정된다
