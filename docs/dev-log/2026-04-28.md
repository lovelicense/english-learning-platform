# 2026-04-28 작업 로그

## 오늘 한 작업
- 모바일 앱 진행 상태를 점검하고 우선 개발 순서를 정리
- 모바일 MVP 티켓 문서 추가
- 모바일 `record` 화면에 실제 녹음 시작/종료, 권한 요청, 경과 시간 표시 구현
- 모바일 `record` 화면에 `recording session` 업로드 연결
  - `presign -> S3 upload -> part complete -> finalize -> process`
- 모바일 `recording/[id]` 상세 화면 구현
  - STT / diarization 결과 조회
  - 화자 목록 표시
  - 문장 목록 표시
- 모바일 `recording/[id]`에서 문장 수정/삭제, 내 화자 선택 연결
- 모바일 `recording/[id]`에서 영어 표현 생성, TTS 생성/재생 연결
- 모바일 `expressions` 탭 목록 / `expression/[id]` 상세 화면 구현
  - 표현 목록 조회
  - 검색
  - TTS 재생
  - 메모 저장
- 모바일 `reviews` 탭 구현
  - 복습 카드 목록 조회
  - 문제 생성
  - 텍스트 답변 채점
  - 최근 연습 기록 조회
- 모바일 API base URL 기본값을 개발 친화적으로 조정
  - Android: `http://10.0.2.2:4000`
  - iOS/web: `http://localhost:4000`
- 모바일 web preview 테스트 중 업로드 이슈 수정
  - `expo-file-system` web 미지원 분기 처리
  - 업로드 파일명 확장자 보정으로 `Unsupported file format` 해결

## 테스트 확인
- 로그인
- 녹음 시작/종료
- 업로드
- STT/diarization 결과 확인
- 문장 수정/삭제
- 내 화자 선택
- 영어 표현 생성
- TTS 생성/재생
- 표현 목록/상세

## 수정한 주요 파일
- `apps/mobile/app/(tabs)/record.tsx`
- `apps/mobile/app/recording/[id].tsx`
- `apps/mobile/app/(tabs)/expressions.tsx`
- `apps/mobile/app/expression/[id].tsx`
- `apps/mobile/app/(tabs)/reviews.tsx`
- `apps/mobile/src/lib/config.ts`
- `apps/mobile/src/lib/api/recording-sessions.ts`
- `apps/mobile/src/lib/api/recordings.ts`
- `apps/mobile/src/lib/api/expressions.ts`
- `apps/mobile/src/lib/api/practice.ts`
- `apps/mobile/src/lib/api/reviews.ts`
- `apps/mobile/package.json`
- `docs/mobile_mvp_tickets.md`
- `docs/project_context.md`

## 이슈 / 해결
- 모바일 로그인 오류 원인 확인
  - 웹은 로컬 API, 모바일은 운영 API 기본값이라 서버가 달랐음
  - 모바일 기본 API 주소를 개발용 기준으로 조정
- 모바일 web preview에서 업로드 시 `Failed to fetch`
  - S3 CORS와 web preview 환경 차이 확인
  - web 경로 업로드 방식 분리
- worker에서 `Unsupported file format`
  - 업로드 파트 파일명에 확장자 누락
  - content-type 기준으로 확장자 자동 보정

## 다음 작업 후보
- 모바일 `reviews` 탭에 음성 답변 채점 붙이기
- 모바일 복습/표현 연습 UX 다듬기
- 모바일 설정 화면에 API 주소 직접 변경 기능 검토
- 모바일 배포용 env / 운영 체크리스트 정리
