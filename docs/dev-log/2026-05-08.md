# 2026-05-08 작업 로그

## 오늘 한 작업
- 모바일 웹 패리티 Ticket P9 완료
  - 모바일 `Learning Assets` 화면에 `데이터 Export` 섹션 추가
  - `한글표현 자산`, `영어표현 자산` 각각 JSON / CSV 저장 액션 추가
  - export 응답의 `Content-Disposition` 파일명을 모바일에서도 읽어 실제 저장 파일명에 반영
- 모바일 export 저장 흐름 추가
  - Android: 저장 폴더 선택 후 선택한 위치에 파일 저장
  - iOS: 앱 문서 디렉터리에 파일 저장 후 공유 시트로 전달
- 모바일 API 클라이언트 보강
  - JSON 응답 전용 `apiFetch` 외에 raw response를 받는 `apiFetchRaw` 추가
  - export 같은 파일/텍스트 응답도 모바일에서 공통 처리 가능하게 정리
- 문서 반영
  - `mobile_web_parity_tickets.md`에 P9 완료 상태 반영
  - `project_context.md` 최신 업데이트에 모바일 export 지원 추가

## 테스트 / 검증
- `pnpm --filter @elp/mobile exec tsc --noEmit`

## 수정한 주요 파일
- `apps/mobile/app/learning-assets.tsx`
- `apps/mobile/src/lib/api/client.ts`
- `apps/mobile/src/lib/api/exports.ts`
- `docs/mobile_web_parity_tickets.md`
- `docs/project_context.md`

## 메모
- 모바일 웹 패리티 남은 핵심 묶음은 이제 `파일 가져오기`, `업로드 실패 복구`, `표현 삭제`다.
- Android export는 시스템 폴더 선택기를 먼저 여는 구조라 실제 기기에서 저장 위치 선택 UX를 한 번 확인하는 것이 좋다.
