# 2026-04-29 작업 로그

## 오늘 한 작업
- 모바일 `reviews` 탭에 `음성 답변(STT)` 모드 추가
- 복습 카드에서 `텍스트 답변` / `음성 답변(STT)` 전환 가능하도록 정리
- 모바일에서 영어 답변 녹음 시작/종료, 권한 요청, 녹음 길이 표시 추가
- 모바일 `reviews`에서 `녹음 종료 -> 자동 STT 업로드 -> 자동 음성 채점` 흐름 추가
- 모바일 `reviews`에서 채점 완료 후 정답 TTS 자동 재생 추가
- 모바일 `reviews`에서 정답 TTS 종료 후 다음 복습 카드로 자동 이동하고 다음 문제를 자동 준비하도록 추가
- 모바일 `reviews` 마지막 카드 완료 시 `오늘 복습 완료` 카드와 다시 시작 / 새로고침 버튼 추가
- 모바일 `reviews` 음성 모드에서 질문 한국어를 TTS로 읽고 3초 뒤 자동 녹음 시작하도록 추가
- 모바일 질문 읽기용 `expo-speech` 의존성 추가
- 모바일 `expression/[id]/practice` 전용 연습 화면 추가
- 모바일 표현 상세 화면에서 `표현 연습` 버튼으로 전용 연습 화면 진입 추가
- 모바일 표현 연습 화면에서 `이전 표현 / 다음 표현` 버튼으로 연속 학습 이동 추가
- 모바일 `reviews` 와 `표현 연습` 모두 문제 유형 칩은 선택만 하고 `문제 생성(시작)` 버튼이 실제 시작 트리거가 되도록 UX 정리
- 모바일 `reviews` 는 채점 후 자동 다음 이동 대신 `다음 문제` 버튼으로 이동하도록 변경
- 음성 답변용 업로드 흐름 연결
  - `POST /practice/voice/presign`
  - presigned URL 업로드
  - `POST /practice/score-voice`
- 음성 채점 결과에 아래 정보 표시 추가
  - STT 인식 문장
  - 채점된 음성 다시 듣기
  - 최근 연습 기록의 voice/STT 정보
- 모바일 practice API 클라이언트에 음성 presign/음성 채점 함수 추가

## 테스트 / 검증
- `pnpm --filter @elp/mobile exec tsc --noEmit` 통과

## 수정한 주요 파일
- `apps/mobile/app/(tabs)/reviews.tsx`
- `apps/mobile/src/lib/api/practice.ts`
- `docs/project_context.md`
- `docs/dev_log.md`

## 메모
- 현재 모바일 `reviews`는 텍스트 답변과 음성 답변 채점을 모두 지원
- 기본 답변 모드는 모바일 사용성을 고려해 `음성 답변(STT)`로 시작
- 다음으로는 `녹음 종료 -> 자동 STT/자동 채점` 여부를 UX 관점에서 검토할 수 있음
